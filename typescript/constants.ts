import { Direction, Action } from "./types";

export const DIRECTIONS = {
  UP: true as Direction,
  DOWN: false as Direction,
};

export const ACTIONS = {
  MOVE: true as Action,
  STOP: false as Action,
};
